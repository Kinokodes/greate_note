#include <string>
#include <vector>

std::vector <int> FindWord(std::string text, std::string word);


int CheckBracket(std::string b);

int CheckBracket(std::string text);
std::string GetSpace(std::string& text);

